# 服务器部署指南 - BSC靓号生成器

本指南帮助你在高配置服务器上部署和运行靓号生成器，以获得最佳性能。

## 一、服务器选择

### 推荐配置

| 目标难度 | CPU核心数 | 内存 | 预计速度 | 适合场景 |
|---------|----------|------|---------|---------|
| 6位靓号 | 8核 | 4GB | 8-12万/s | 入门级 |
| 7位靓号 | 16核 | 8GB | 15-25万/s | 进阶级 |
| 8位靓号 | 32核 | 16GB | 30-50万/s | 高级 |
| 9位靓号 | 64核 | 32GB | 60-100万/s | 专业级 |
| 10位+ | 96核+ | 64GB+ | 100万+/s | 超级靓号 |

### 云平台推荐

#### AWS EC2

```bash
# 推荐实例类型
# c7i系列 - 最新计算优化型
c7i.8xlarge    # 32核，适合8位
c7i.16xlarge   # 64核，适合9位
c7i.24xlarge   # 96核，适合10位

# 使用竞价实例节省成本
# 命令行创建竞价实例：
aws ec2 run-instances \
    --image-id ami-xxxxxxxxx \
    --instance-type c7i.16xlarge \
    --instance-market-options MarketType=spot \
    --key-name your-key
```

#### 阿里云 ECS

```bash
# 推荐实例规格族
ecs.c7.8xlarge    # 32核
ecs.c7.16xlarge   # 64核

# 使用抢占式实例节省成本
# 可在控制台创建，选择"抢占式实例"
# 折扣通常在60-90%
```

#### 腾讯云 CVM

```bash
# 推荐实例规格
C5.8XLARGE128    # 32核
C5.16XLARGE128   # 64核

# 使用竞价实例
# 在购买页面选择"竞价实例"
```

## 二、系统环境配置

### 1. 连接服务器

```bash
# 使用SSH连接
ssh -i your-key.pem user@server-ip

# 或使用密码
ssh user@server-ip
```

### 2. 安装Python环境

#### Ubuntu/Debian

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python和pip
sudo apt install python3 python3-pip -y

# 验证安装
python3 --version
pip3 --version
```

#### CentOS/RHEL

```bash
# 更新系统
sudo yum update -y

# 安装Python
sudo yum install python3 python3-pip -y

# 验证安装
python3 --version
pip3 --version
```

### 3. 安装必要工具

```bash
# 安装screen（用于后台运行）
sudo apt install screen -y  # Ubuntu/Debian
sudo yum install screen -y  # CentOS/RHEL

# 或安装tmux
sudo apt install tmux -y    # Ubuntu/Debian
sudo yum install tmux -y    # CentOS/RHEL
```

## 三、部署程序

### 方法1：直接上传（推荐）

```bash
# 在本地，使用scp上传文件
scp -i your-key.pem vanity_wallet_generator.py user@server-ip:~/
scp -i your-key.pem fast_generator.py user@server-ip:~/
scp -i your-key.pem requirements.txt user@server-ip:~/
```

### 方法2：使用Git

```bash
# 在服务器上
cd ~
git clone your-repo-url
cd your-repo-directory
```

### 方法3：手动创建

```bash
# 在服务器上创建文件
nano vanity_wallet_generator.py
# 复制粘贴代码内容
# Ctrl+X, Y, Enter 保存退出

# 创建requirements.txt
nano requirements.txt
# 复制粘贴依赖内容
# Ctrl+X, Y, Enter 保存退出
```

### 4. 安装依赖

```bash
# 安装Python依赖
pip3 install -r requirements.txt

# 如果速度慢，使用国内镜像
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 四、后台运行

### 使用 Screen（推荐）

```bash
# 创建新会话
screen -S vanity

# 运行程序
python3 fast_generator.py
# 或
python3 vanity_wallet_generator.py

# 按照提示输入配置
# 模式: 1 (prefix)
# 匹配: 88888888
# 大小写: n
# 数量: 1
# 进程: 64 (根据你的CPU核心数)

# 分离会话（程序继续运行）
# 按 Ctrl+A，然后按 D

# 重新连接查看进度
screen -r vanity

# 列出所有会话
screen -ls

# 结束会话
screen -X -S vanity quit
```

### 使用 Tmux

```bash
# 创建新会话
tmux new -s vanity

# 运行程序
python3 fast_generator.py

# 分离会话
# 按 Ctrl+B，然后按 D

# 重新连接
tmux attach -t vanity

# 列出所有会话
tmux ls

# 结束会话
tmux kill-session -t vanity
```

### 使用 Nohup

```bash
# 创建自动输入脚本
cat > input.txt << EOF
1
88888888
n
1
64
y
EOF

# 后台运行
nohup python3 fast_generator.py < input.txt > output.log 2>&1 &

# 查看进程
ps aux | grep fast_generator

# 查看输出
tail -f output.log

# 停止进程
kill <PID>
```

## 五、监控和管理

### 1. 监控进程

```bash
# 查看CPU使用率
top
# 按1显示每个核心
# 按q退出

# 查看进程详情
htop  # 需要安装：sudo apt install htop

# 查看Python进程
ps aux | grep python
```

### 2. 监控输出

```bash
# 如果使用screen/tmux，重新连接即可看到实时输出
screen -r vanity

# 如果使用nohup
tail -f output.log

# 查看结果文件
cat super_vanity_wallets.txt
# 或
cat vanity_wallets.txt
```

### 3. 资源监控

```bash
# 安装监控工具
sudo apt install htop iotop -y

# CPU监控
htop

# 内存监控
free -h

# 磁盘监控
df -h
```

## 六、性能优化

### 1. 进程数优化

```bash
# 查看CPU核心数
nproc

# 推荐配置
进程数 = CPU核心数

# 如果是超线程CPU，可以尝试
进程数 = CPU核心数 × 1.5
进程数 = CPU核心数 × 2
```

### 2. 优先级调整

```bash
# 提高进程优先级（需要root）
sudo nice -n -10 python3 fast_generator.py

# 或在运行中调整
sudo renice -n -10 -p <PID>
```

### 3. CPU亲和性设置

```bash
# 绑定到特定CPU核心（高级）
taskset -c 0-31 python3 fast_generator.py
```

## 七、成本优化

### 1. 使用竞价实例

```bash
# 优势：
# - 价格低60-90%
# - 适合可中断任务

# 注意：
# - 可能被系统回收
# - 需要设置中断处理
# - 建议使用screen/tmux防止数据丢失
```

### 2. 按需使用

```bash
# 只在需要时创建实例
# 完成后立即删除
# 避免长期占用

# AWS CLI示例
aws ec2 terminate-instances --instance-ids i-xxxxxx
```

### 3. 选择合适区域

```bash
# 不同区域价格不同
# 通常亚太区域较贵
# 美国区域较便宜
# 根据需求选择
```

## 八、批量生成

### 创建批量脚本

```bash
# 创建批量配置文件
cat > batch_config.txt << EOF
prefix,888888,n,1,64
prefix,666666,n,1,64
prefix,999999,n,1,64
suffix,888888,n,1,64
contains,aaaaaa,n,5,64
EOF

# 创建批量运行脚本
cat > batch_run.sh << 'EOF'
#!/bin/bash

while IFS=',' read -r mode pattern case_sens num procs; do
    echo "开始生成: $mode - $pattern"
    
    # 转换mode
    if [ "$mode" = "prefix" ]; then
        mode_num=1
    elif [ "$mode" = "suffix" ]; then
        mode_num=2
    else
        mode_num=3
    fi
    
    # 创建输入
    echo -e "${mode_num}\n${pattern}\n${case_sens}\n${num}\n${procs}\ny" | python3 fast_generator.py
    
    echo "完成: $mode - $pattern"
    echo "---"
done < batch_config.txt
EOF

chmod +x batch_run.sh

# 运行批量任务
screen -S batch
./batch_run.sh
# Ctrl+A, D分离
```

## 九、安全注意事项

### 1. 文件权限

```bash
# 限制结果文件权限
chmod 600 super_vanity_wallets.txt
chmod 600 vanity_wallets.txt

# 限制脚本权限
chmod 700 *.py
```

### 2. 下载结果

```bash
# 生成完成后立即下载到本地
scp -i your-key.pem user@server-ip:~/super_vanity_wallets.txt ./

# 下载后删除服务器上的文件
ssh user@server-ip "rm ~/super_vanity_wallets.txt"
```

### 3. 清理环境

```bash
# 任务完成后清理
rm -rf *.py
rm -rf *.txt
history -c  # 清除命令历史

# 如果是竞价实例，直接删除
```

## 十、故障排除

### 常见问题

#### 1. 依赖安装失败

```bash
# 使用国内镜像
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或单独安装
pip3 install eth-keys eth-utils pycryptodome -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 2. 内存不足

```bash
# 添加swap
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 验证
free -h
```

#### 3. 进程被杀死

```bash
# 检查系统日志
dmesg | grep -i kill

# 检查内存
free -h

# 减少进程数重试
```

#### 4. SSH连接断开

```bash
# 这就是为什么要用screen/tmux
# 重新连接后执行
screen -r vanity
# 程序仍在运行
```

## 十一、性能基准

### 实测数据（参考）

| 服务器配置 | 进程数 | 实测速度 | 8位靓号耗时 | 9位靓号耗时 |
|-----------|--------|---------|-----------|-----------|
| 8核 3.0GHz | 8 | 12万/s | 10小时 | 6天 |
| 16核 3.2GHz | 16 | 25万/s | 5小时 | 3天 |
| 32核 3.5GHz | 32 | 48万/s | 2.5小时 | 1.5天 |
| 64核 3.8GHz | 64 | 95万/s | 1.2小时 | 18小时 |

*注：实际速度受CPU型号、内存、运气等因素影响*

## 十二、自动化脚本

### 一键部署脚本

```bash
cat > deploy.sh << 'EOF'
#!/bin/bash

echo "开始部署BSC靓号生成器..."

# 更新系统
sudo apt update

# 安装Python
sudo apt install python3 python3-pip screen -y

# 安装依赖
pip3 install eth-keys eth-utils pycryptodome

echo "部署完成！"
echo "运行命令: screen -S vanity"
echo "然后执行: python3 fast_generator.py"
EOF

chmod +x deploy.sh
./deploy.sh
```

## 十三、成本估算

### 8位靓号成本示例

```
AWS c7i.16xlarge (64核)
- 按需价格: $2.80/小时
- 竞价价格: ~$0.84/小时 (70%折扣)
- 预计耗时: 1.5小时
- 总成本: ~$1.26

阿里云 ecs.c7.16xlarge (64核)
- 按需价格: ¥15/小时
- 抢占式: ~¥4.5/小时 (70%折扣)
- 预计耗时: 1.5小时
- 总成本: ~¥6.75
```

### 9位靓号成本示例

```
AWS c7i.16xlarge (64核)
- 竞价价格: ~$0.84/小时
- 预计耗时: 20小时
- 总成本: ~$16.8

使用c7i.24xlarge (96核)更快:
- 竞价价格: ~$1.26/小时
- 预计耗时: 12小时
- 总成本: ~$15.12
```

## 总结

1. **选择合适配置**：根据靓号难度选择CPU核心数
2. **使用竞价实例**：节省60-90%成本
3. **后台运行**：使用screen或tmux防止中断
4. **及时下载**：生成完成立即下载结果
5. **清理环境**：删除服务器上的敏感文件
6. **监控进度**：定期检查生成进度
7. **优化性能**：调整进程数以获得最佳速度

祝你生成成功！🎉



