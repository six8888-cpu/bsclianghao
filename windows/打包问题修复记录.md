# 打包问题修复记录

## ❌ 问题描述

**报错信息**：
```
Traceback (most recent call last):
  File "ultra_generator_windows.py", line 14, in <module>
ModuleNotFoundError: No module named 'eth_utils'
[PYI-10608:ERROR] Failed to execute script 'ultra_generator_windows' due to unhandled exception!
```

**问题原因**：
1. `requirements.txt` 文件为空，导致GitHub Actions没有安装依赖
2. PyInstaller打包时没有包含隐藏导入的加密库

---

## ✅ 解决方案

### 1. 修复 `requirements.txt`（根目录）

**之前**：
```
（空文件）
```

**现在**：
```
eth-keys>=0.4.0
eth-utils>=2.2.0
pycryptodome>=3.15.0
```

---

### 2. 修复 `.github/workflows/build-windows.yml`

**添加隐藏导入参数**：
```yaml
- name: 打包EXE
  run: |
    cd windows
    pyinstaller --onefile \
                --name=BSC-Vanity-Generator \
                --console \
                --clean \
                --noconfirm \
                --hidden-import=eth_keys \
                --hidden-import=eth_utils \
                --hidden-import=Crypto \
                --hidden-import=Crypto.Hash \
                --hidden-import=Crypto.Hash.keccak \
                ultra_generator_windows.py
```

---

### 3. 修复 `windows/build.bat`（Windows本地打包）

**更新命令**：
```batch
pyinstaller --onefile ^
            --name=BSC-Vanity-Generator ^
            --console ^
            --clean ^
            --noconfirm ^
            --hidden-import=eth_keys ^
            --hidden-import=eth_utils ^
            --hidden-import=Crypto ^
            --hidden-import=Crypto.Hash ^
            --hidden-import=Crypto.Hash.keccak ^
            ultra_generator_windows.py
```

---

### 4. 修复 `windows/build_exe.py`

**更新PyInstaller参数**：
```python
cmd = [
    'pyinstaller',
    '--onefile',
    '--name=BSC-Vanity-Generator',
    '--console',
    '--clean',
    '--noconfirm',
    '--hidden-import=eth_keys',
    '--hidden-import=eth_utils',
    '--hidden-import=Crypto',
    '--hidden-import=Crypto.Hash',
    '--hidden-import=Crypto.Hash.keccak',
    'ultra_generator_windows.py'
]
```

---

## 📝 关键点解释

### 为什么需要 `--hidden-import`？

PyInstaller通过静态分析来查找依赖，但某些动态导入可能无法被检测到：

1. **eth_keys** 和 **eth_utils**：BSC钱包生成的核心库
2. **Crypto**：pycryptodome加密库
3. **Crypto.Hash.keccak**：Keccak哈希算法（以太坊地址生成需要）

这些库虽然在代码中导入了，但PyInstaller可能因为复杂的导入路径而遗漏。

---

## 🎯 验证修复

### GitHub Actions自动构建

1. 访问：https://github.com/six8888-cpu/bsclianghao/actions
2. 查看最新的 "Build Windows EXE" 任务
3. 等待绿色对勾 ✓
4. 下载 `BSC-Vanity-Generator-Windows-x64.zip`
5. 解压并运行 `BSC-Vanity-Generator.exe`

### 本地Windows打包（可选）

在Windows电脑上：
```batch
cd windows
build.bat
```

---

## 📦 生成的EXE文件

**文件名**：`BSC-Vanity-Generator.exe`  
**大小**：约30-50MB（包含所有依赖）  
**特点**：
- ✅ 无需Python环境
- ✅ 无需安装依赖
- ✅ 双击即可运行
- ✅ 所有功能完整

---

## 🔄 更新历史

| 时间 | 版本 | 说明 |
|------|------|------|
| 2025-11-17 | v1.0 | 首次发现并修复打包依赖问题 |
| 2025-11-17 | v1.1 | 修复文件名包含中文导致下载失败 |
| 2025-11-17 | v1.2 | 添加隐藏导入，解决运行时依赖缺失 |

---

## 💡 后续优化建议

### 1. 创建专用 `.spec` 文件

可以创建一个 `BSC-Vanity-Generator.spec` 文件，更精确地控制打包：

```python
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['ultra_generator_windows.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[
        'eth_keys',
        'eth_utils',
        'Crypto',
        'Crypto.Hash',
        'Crypto.Hash.keccak',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='BSC-Vanity-Generator',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
```

### 2. 添加图标

创建 `icon.ico` 并在打包时指定：
```
--icon=icon.ico
```

### 3. 添加版本信息

创建 `version_info.txt`，为EXE添加元数据。

---

## 📞 问题排查

如果打包后的EXE仍然报错：

### 1. 检查依赖是否完整
```bash
pip list | grep eth
pip list | grep Crypto
```

### 2. 手动测试导入
```bash
python
>>> import eth_keys
>>> import eth_utils
>>> from Crypto.Hash import keccak
>>> print("所有依赖正常")
```

### 3. 查看PyInstaller详细日志
```bash
pyinstaller --onefile --log-level=DEBUG ultra_generator_windows.py
```

### 4. 测试打包后的EXE
在Windows上运行：
```cmd
BSC-Vanity-Generator.exe
```

---

## ✅ 修复状态

- [x] 添加 requirements.txt
- [x] 更新 GitHub Actions
- [x] 更新本地打包脚本
- [x] 添加隐藏导入
- [x] 修改文件名为英文
- [x] 推送到GitHub
- [x] 等待自动构建完成

---

**最后更新**：2025-11-17  
**修复提交**：941ed2b  
**状态**：✅ 已修复，等待GitHub Actions构建

